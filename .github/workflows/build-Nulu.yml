name: Build NuLu
on: [workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: build-NuLu.yaml   # ← B用ビルドリスト
      config_path: config                 # ← B用設定フォルダ
